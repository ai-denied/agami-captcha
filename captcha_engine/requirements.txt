fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
pydantic-settings>=2.0
sqlalchemy>=2.0
asyncpg>=0.29
redis>=5.0
firebase-admin>=6.0
# 로그인 사용자 accessToken(JWT, HS256) 검증용. firebase-admin 이 전이 의존성으로
# 끌어오지만(현재 2.13), 직접 import 하므로 명시적으로 선언.
PyJWT>=2.0
python-multipart>=0.0.9
asyncpg
psycopg2-binary
# 손전등 봇 위험도 추론을 같은 클러스터의 추론 마이크로서비스 HTTP 호출로 위임.
# (ONNX 추론은 제거됐으나 face 피처 추출에 numpy 필요)
httpx>=0.27
numpy>=1.26
